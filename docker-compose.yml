services:
  client:
    build: ./client
    ports:
      - "3000:3000"
    env_file:
      - ./client/.env.local
  server:
    build: ./server
    ports:
      - "8000:8000"
    env_file:
      - ./server/.env